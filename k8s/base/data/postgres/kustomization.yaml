apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - service.yaml
  - statefulset.yaml

# This grabs your bash script and turns it into a volume-mountable ConfigMap!
configMapGenerator:
  - name: postgres-init-script
    files:
      - init-dbs.sh